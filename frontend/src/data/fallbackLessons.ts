// Bundled with the frontend so every lesson remains available if the API is offline.
export { getLessonById, getLessonsByLanguage, isLanguageCode, languages, lessons } from "../../../shared/lessonData";
