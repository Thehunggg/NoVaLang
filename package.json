{
  "name": "novalang",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently -n API,WEB -c yellow,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "generate:curriculum": "node scripts/generate-curriculum.mjs",
    "sync:flutter-assets": "node scripts/sync-shared-assets.mjs",
    "sync:curriculum": "npm run generate:curriculum && npm run sync:flutter-assets"
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
