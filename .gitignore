node_modules/
dist/
build/
.env
.env.local
.DS_Store
.vscode/

# NovaLang language-rule pipeline (/build-language)
# Dataset/corpus tải về là cache, KHÔNG commit (repo nằm trong OneDrive).
tools/cache/
rules/**/corpus/
*.corpus.txt
*.dataset.json

# Local-only source materials (KHÔNG commit)
local-sources/

# Bản sao lưu nguồn trước mỗi ngoại lệ sửa (G14-R13) — cùng lý do, không commit
local-sources-backup/

# Cấu hình chạy dev server tại máy — mỗi máy một kiểu, không commit
.claude/launch.json

# Trang duyệt tĩnh sinh ra từ scripts/preview-lesson.mjs — sinh lại được
scripts/preview/
